# Square Root & Reciprocal

Integer square root (combinational non-restoring, iterative digit-by-digit, and Newton's method) and reciprocal-square-root.

*Directory:* `src/sqrt_reciprocal/` -- 16 modules.

| Module | Description |
|--------|-------------|
| `rsqrt_comb16` | 16-bit reciprocal square root (structural sqrt + structural reciprocal divide). |
| `rsqrt_comb32` | 32-bit reciprocal square root (structural sqrt + structural reciprocal divide). |
| `rsqrt_comb4` | 4-bit reciprocal square root (structural sqrt + structural reciprocal divide). |
| `rsqrt_comb8` | 8-bit reciprocal square root (structural sqrt + structural reciprocal divide). |
| `sqrt_comb16` | 16-bit integer square root (structural digit-by-digit non-restoring array). |
| `sqrt_comb32` | 32-bit integer square root (structural digit-by-digit non-restoring array). |
| `sqrt_comb4` | 4-bit integer square root (structural digit-by-digit non-restoring array). |
| `sqrt_comb8` | 8-bit integer square root (structural digit-by-digit non-restoring array). |
| `sqrt_iter16` | 16-bit iterative integer square root (digit-by-digit). |
| `sqrt_iter32` | 32-bit iterative integer square root (digit-by-digit). |
| `sqrt_iter4` | 4-bit iterative integer square root (digit-by-digit). |
| `sqrt_iter8` | 8-bit iterative integer square root (digit-by-digit). |
| `sqrt_newton16` | 16-bit Newton's-method square root. |
| `sqrt_newton32` | 32-bit Newton's-method square root. |
| `sqrt_newton4` | 4-bit Newton's-method square root. |
| `sqrt_newton8` | 8-bit Newton's-method square root. |

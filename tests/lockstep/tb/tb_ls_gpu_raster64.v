// auto-generated by scripts/gen_lockstep.py -- do not edit
`timescale 1ns/1ps
module tb_ls_gpu_raster64;
    reg clk;
    reg reset;
    reg gpu_we;
    reg [3:0] gpu_addr;
    reg [31:0] gpu_wdata;
    reg screen_ready;
    wire [31:0] d_gpu_rdata;
    wire [31:0] g_gpu_rdata;
    wire d_frame_start;
    wire g_frame_start;
    wire d_frame_done;
    wire g_frame_done;
    wire d_enable;
    wire g_enable;
    wire d_fill;
    wire g_fill;
    wire d_mono_valid;
    wire g_mono_valid;
    wire [5:0] d_mono_y;
    wire [5:0] g_mono_y;
    wire [63:0] d_mono_s0;
    wire [63:0] g_mono_s0;
    integer i, errors;
    integer seed;
    reg [31:0] r;
    reg [63:0] rr;
    initial errors = 0;

    gpu_raster64 dut(
        .clk(clk),
        .reset(reset),
        .gpu_we(gpu_we),
        .gpu_addr(gpu_addr),
        .gpu_wdata(gpu_wdata),
        .screen_ready(screen_ready),
        .gpu_rdata(d_gpu_rdata),
        .frame_start(d_frame_start),
        .frame_done(d_frame_done),
        .enable(d_enable),
        .fill(d_fill),
        .mono_valid(d_mono_valid),
        .mono_y(d_mono_y),
        .mono_s0(d_mono_s0)
    );

    gold_gpu_raster64 gut(
        .clk(clk),
        .reset(reset),
        .gpu_we(gpu_we),
        .gpu_addr(gpu_addr),
        .gpu_wdata(gpu_wdata),
        .screen_ready(screen_ready),
        .gpu_rdata(g_gpu_rdata),
        .frame_start(g_frame_start),
        .frame_done(g_frame_done),
        .enable(g_enable),
        .fill(g_fill),
        .mono_valid(g_mono_valid),
        .mono_y(g_mono_y),
        .mono_s0(g_mono_s0)
    );

    initial clk = 0;
    always #5 clk = ~clk;

    initial begin
        seed = 32'd85780;
        gpu_we = 0;
        gpu_addr = 0;
        gpu_wdata = 0;
        screen_ready = 0;
        reset = 1;
        repeat (5) @(posedge clk);
        reset = 0;
        for (i = 0; i < 2000; i = i + 1) begin
            @(negedge clk);
            r = $random(seed); gpu_we = r[0:0];
            r = $random(seed); gpu_addr = r[3:0];
            r = $random(seed); gpu_wdata = r[31:0];
            r = $random(seed); screen_ready = r[0:0];
            @(posedge clk); #1;
            if ({d_gpu_rdata, d_frame_start, d_frame_done, d_enable, d_fill, d_mono_valid, d_mono_y, d_mono_s0} !== {g_gpu_rdata, g_frame_start, g_frame_done, g_enable, g_fill, g_mono_valid, g_mono_y, g_mono_s0}) begin
                errors = errors + 1;
                if (errors <= 5)
                    $display("MISMATCH gpu_raster64 @cycle %0d", i);
            end
        end
        if (errors == 0) $display("PASS gpu_raster64 lockstep (2000 cycles)");
        else $display("FAIL gpu_raster64: %0d mismatching cycles", errors);
        $finish;
    end
endmodule

// auto-generated by scripts/gen_lockstep.py -- do not edit
`timescale 1ns/1ps
module tb_ls_gpu_sprite16;
    reg clk;
    reg reset;
    reg gpu_we;
    reg [3:0] gpu_addr;
    reg [31:0] gpu_wdata;
    reg screen_ready;
    wire [31:0] d_gpu_rdata;
    wire [31:0] g_gpu_rdata;
    wire d_frame_start;
    wire g_frame_start;
    wire d_frame_done;
    wire g_frame_done;
    wire d_enable;
    wire g_enable;
    wire d_fill;
    wire g_fill;
    wire d_px_valid;
    wire g_px_valid;
    wire [3:0] d_px_x;
    wire [3:0] g_px_x;
    wire [3:0] d_px_y;
    wire [3:0] g_px_y;
    wire [3:0] d_px_mux;
    wire [3:0] g_px_mux;
    integer i, errors;
    integer seed;
    reg [31:0] r;
    reg [63:0] rr;
    initial errors = 0;

    gpu_sprite16 dut(
        .clk(clk),
        .reset(reset),
        .gpu_we(gpu_we),
        .gpu_addr(gpu_addr),
        .gpu_wdata(gpu_wdata),
        .screen_ready(screen_ready),
        .gpu_rdata(d_gpu_rdata),
        .frame_start(d_frame_start),
        .frame_done(d_frame_done),
        .enable(d_enable),
        .fill(d_fill),
        .px_valid(d_px_valid),
        .px_x(d_px_x),
        .px_y(d_px_y),
        .px_mux(d_px_mux)
    );

    gold_gpu_sprite16 gut(
        .clk(clk),
        .reset(reset),
        .gpu_we(gpu_we),
        .gpu_addr(gpu_addr),
        .gpu_wdata(gpu_wdata),
        .screen_ready(screen_ready),
        .gpu_rdata(g_gpu_rdata),
        .frame_start(g_frame_start),
        .frame_done(g_frame_done),
        .enable(g_enable),
        .fill(g_fill),
        .px_valid(g_px_valid),
        .px_x(g_px_x),
        .px_y(g_px_y),
        .px_mux(g_px_mux)
    );

    initial clk = 0;
    always #5 clk = ~clk;

    initial begin
        seed = 32'd18310;
        gpu_we = 0;
        gpu_addr = 0;
        gpu_wdata = 0;
        screen_ready = 0;
        reset = 1;
        repeat (5) @(posedge clk);
        reset = 0;
        for (i = 0; i < 2000; i = i + 1) begin
            @(negedge clk);
            r = $random(seed); gpu_we = r[0:0];
            r = $random(seed); gpu_addr = r[3:0];
            r = $random(seed); gpu_wdata = r[31:0];
            r = $random(seed); screen_ready = r[0:0];
            @(posedge clk); #1;
            if ({d_gpu_rdata, d_frame_start, d_frame_done, d_enable, d_fill, d_px_valid, d_px_x, d_px_y, d_px_mux} !== {g_gpu_rdata, g_frame_start, g_frame_done, g_enable, g_fill, g_px_valid, g_px_x, g_px_y, g_px_mux}) begin
                errors = errors + 1;
                if (errors <= 5)
                    $display("MISMATCH gpu_sprite16 @cycle %0d", i);
            end
        end
        if (errors == 0) $display("PASS gpu_sprite16 lockstep (2000 cycles)");
        else $display("FAIL gpu_sprite16: %0d mismatching cycles", errors);
        $finish;
    end
endmodule
